@model Customer

<h3 class="p-2 bg-primary text-white text-center">Клиенты</h3>
<form asp-action="UpdateCustomer" method="post">
    <div class="form-group">
        <label asp-for="Id"></label>
        <input asp-for="Id" class="form-control" readonly/>
    </div>
    <div class="form-group">
        <label asp-for="Name"></label>
        <input asp-for="Name" class="form-control" />
    </div>
    <div class="form-group">
        <label asp-for="Phone"></label>
        <input asp-for="Phone" class="form-control" />
    </div>
    <div class="form-group">
        <label asp-for="Email"></label>
        <input asp-for="Email" class="form-control" />
    </div>
@*    <div class="form-group">
        <label asp-for="Car"></label>
        <select class="form-control" asp-for="CarId">
            @if (Model.Id == 0)
            {
                <option disabled selected>Выберите машину</option>
            }
            @foreach(Car c in ViewBag.Cars)
            {
                <option selected="@(Model.Car?.Id == c.Id)" 
                value="@c.Id">@c.Name</option>
            }
        </select>
    </div>*@

    <div>
        <button class="btn btn-primary" type="submit">Сохранить</button>
        <a asp-action="Index" class="btn btn-danger">Отмена</a>
    </div>
</form>